# AI Agent Guide for FinanceFlow

## Project Overview
This is a Next.js project using Firebase App Hosting.

## 🚀 Deployment Rule (CRITICAL)
**Deployment is AUTOMATED via Git Push.**
- **DO NOT** ask the user for a deployment script.
- **DO NOT** try to run `deploy_manual.ps1` (it does not exist).
- Just `git push` and Firebase will handle the rest.

## Environment & Infrastructure
- **Shell**: PowerShell 7 (`pwsh`)
- **Package Managers**:
  - Frontend: `pnpm`
  - Backend/Python: `uv`
- **Line Endings**: LF

## Code Standards
- **Framework**: Next.js (App Router)
- **Styling**: Tailwind CSS + shadcn/ui
- **Language**: TypeScript (`.tsx`, `.ts`)
